# 設計判断ログ

- 目的：下位モデル（実装者）から報告された仕様の曖昧点・未決事項に対し、Fable が下した判断を記録する。仕様書に反映済みのものはその旨を記す。
- 判断日：2026-09-09

## M0（TASK-001〜005）の判断

| # | 出所 | 論点 | 判断 | 反映先 |
|---|---|---|---|---|
| J-01 | TASK-002 | usage_class が null の建物の扱い | 対象外にせず `assessed`。I 基礎点は other と同じ 1。用途不明で除外すると見逃しが増えるため | 03 仕様 2 章に追記 |
| J-02 | TASK-002 | 高潮データのみの建物を `insufficient_data` 扱い | **不採用**。内水・洪水・高潮のすべてが null の場合のみ評価不能とする。修正を指示 | 03 仕様 3-2・10 章を修正、コード修正 |
| J-03 | TASK-002 | H=0 かつ実績ありで H=2 の適用範囲 | 採用。区域外が確定した建物に適用。内水・洪水とも null で実績のみの建物は暫定 H=2 とし、地形補正を重ねない | 03 仕様 3-3 に追記 |
| J-04 | TASK-002 | Tier 1 上書きの適用順序 | 採用。Q1/Q2/Q6〜Q10 を先に適用し、Q3（決定的）と「Q3=いいえ かつ Q4=はい かつ Q5=いいえ」を最後に適用 | 03 仕様 4-3 に追記 |
| J-05 | TASK-002 | `insufficient_data` 時の出力 | 採用。H・P・優先度は null、V・I・C は算出し、不足情報・対策候補も出す | 03 仕様 10 章に追記 |
| J-06 | TASK-002 | evidence の出典粒度（features に項目別の出典・取得日がない） | 採用。MVP は `data_versions` を出典とする。フェーズ 2 で features に `source_<field>` 列を追加する | 02 要件 16 章に検討事項として追記 |
| J-07 | TASK-002 | None は閾値条件を満たさない（False 扱い） | 採用 | 03 仕様 1 章に追記 |
| J-08 | TASK-003 | PLATEAU usage コード表が未検証 | 採用（仮表のまま）。V-12 として検証項目に追加 | 02 要件 16 章 |
| J-09 | TASK-003 | P04/P14/N02 フラグが usage_code を上書きする優先順位 | 採用。施設データのフラグを優先する（用途コードより施設台帳が確実） | pipelines README |
| J-10 | TASK-003 | datacenter の判定手段がない | 採用（常に False）。データセンターは管理施設登録（FR-08）で usage_class を上書きできるようにする | 02 要件 FR-08 に追記 |
| J-11 | TASK-003 | alt_facility_dist_m の「同用途」粒度 | 採用（usage_class 完全一致）。商業は commercial と commercial_large を同一視するのが望ましく、M2 で変更 | pipelines README |
| J-12 | TASK-003 | landform_class の結合未実装（V-06 待ち） | 採用 | — |
| J-13 | TASK-003 | depression_flag の暫定指標 | 採用。M2 で WhiteboxTools/RichDEM に差し替え | — |
| J-14 | TASK-003 | 30_terrain.py の距離計算が EPSG:4326 簡易実装 | 採用しない。DR-02 に従い EPSG:6677 に投影して計算するよう M2 で修正（TODO） | pipelines README に TODO |
| J-15 | TASK-003 | hazard_source_year が建物単位で単一値 | 採用（最も古いソースの年を入れる。保守的） | 03 仕様 7 章に追記 |
| J-16 | TASK-005 | 「要確認のみ」フィルタの定義 | 採用。`priority_raised_by_low_confidence = true` の建物 | 02 要件 FR-05 に追記 |
| J-17 | TASK-005 | buildings.geojson に usage_class を持たせる | 採用（表示用の非正規化。正は features） | — |
| J-18 | TASK-001 | P02/P05 の URL 未確認 | V-11 として追加済み | 02 要件 16 章 |
| J-19 | TASK-001 | 地下出入口データの受け皿がない | フェーズ 2 で `underground_entrance_flag` を追加 | 02 要件 6-1 |

## M1 準備（TASK-006）の判断

| # | 出所 | 論点 | 判断 | 反映先 |
|---|---|---|---|---|
| J-20 | TASK-006 | V-01 で PLATEAU 建物付与浸水リスク属性の非 null 率に閾値を置くか | 置かない。リスク属性は浸水想定区域内の建物にしか付与されない設計のため、生の非 null 率は品質指標にならない。MVP では東京都浸水予想区域図との空間結合を H の主ソースとし、PLATEAU 属性は照合用（両者が食い違う建物を V-02 の階級写像の検証に使う）。地下階数の 80% 基準は維持 | 07 手順書 V-01、04 カタログ |
| J-21 | TASK-006 | V-03 の DEM 到達率閾値と dem5b の位置づけ | dem5a 到達率 95% 以上なら dem5a 単独。未満なら dem5a → dem5b → dem10b の順で欠損タイルを補完し、`ground_elev_m` の出典を建物ごとに記録する。dem1a が対象区で提供されていれば M2 で優先ソースに切り替える | 07 手順書 V-03、pipelines/30_terrain.py の TODO |
| J-22 | TASK-006 | V-04 で浸水実績がダウンロードできない場合 | 目黒区・世田谷区の浸水実績図 PDF を手動でジオリファレンスしてポリゴン化する。優先度は必須（H 補正と検証集合の両方に要る）。目安は 2 区で 1〜2 人日。区ごとに 1 GeoJSON、属性は event_year と source | 07 手順書 V-04、pipelines/50_history.py |
| J-23 | TASK-006 | V-06 で地形分類タイルが使えない場合 | `landform_class` は null のままとし、H の地形補正は DEM 由来の `depression_flag` と `rel_elev_m` だけで行う（仕様 3-3 はすでに両方を含む）。DEM 自前解析の高度化はフェーズ 2（J-13） | 07 手順書 V-06 |
| J-24 | TASK-006 | V-09 重ねるハザードマップをカタログに載せるか | 載せる。「任意・クロスチェック用」の行を追加し、内水タイルの URL と凡例が確認できたら埋める | 04 カタログ、data/catalog.json |
| J-25 | TASK-006 | V-10 商用利用可否が明文でないデータの扱い | PoC（社内利用）では使用可。外販・外部公開の前に、明文確認できたデータだけに絞る「公開前チェック」を設ける。`commercial_use: unknown` はそのまま残し、公開時のブロッカーとして扱う | 04 カタログ 第 4 章、02 要件 D-15 |
| J-26 | TASK-006 | V-11 で P02/P05 が特定できない場合の `public_flag` | PLATEAU `bldg:usage` の官公庁施設コードと、東京都・区のオープンデータ公共施設一覧（住所ジオコーディング）で代替生成する。両方ない建物は null | 07 手順書 V-11、pipelines/40_context.py |

## 環境制約に起因する未完了事項

- 実データのダウンロードと GIS 処理は本環境では実行不可。`docs/04_データカタログ.md` 第 5 章の手順をローカル環境で実施し、V-01〜V-12 を検証する。
- ビューアはブラウザ未確認。ローカルで `python3 -m http.server` により確認する。
