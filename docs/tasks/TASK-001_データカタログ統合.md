# TASK-001 データカタログ統合と検証事例集

- 担当：Sonnet 5（調査員） 発注：Fable
- 目的：調査レポート A〜D を、要件定義書 6-1 の優先順位に沿った単一のデータ台帳（Markdown + JSON）と、検証事例集に統合する。
- 前提・制約：
  - WebFetch・curl は使えない。追加調査は WebSearch のみ。基本は既存レポートの統合作業。
  - 推測と確認済みを混ぜない。各行に「確からしさ」列を必ず持つ。
  - PyYAML は使わず JSON を書く。
- 入力：
  - `docs/02_要件定義書.md` 第 6 章・第 12 章・第 16 章
  - `docs/research/A_建物データ調査.md`、`B_ハザードデータ調査.md`、`C_地形基盤データ調査.md`、`D_事例競合基準調査.md`
- 出力：
  1. `docs/04_データカタログ.md`：
     - 第 1 章：MVP 必須データ（要件定義 6-1 の順）。各データに：データ名 / source_id（英小文字スネークケース、例 `plateau_meguro_2025`）/ 提供元 / 対象範囲・年度 / 形式・座標系 / 取得 URL / ライセンス・商用利用 / 本システムでの用途（どの features フィールドに入るか。`config/features_schema.json` のフィールド名で書く）/ 確からしさ / 要検証 ID（要件定義 16 章の V-xx）
     - 第 2 章：推奨・任意データ（同形式）
     - 第 3 章：フェーズ 2・3 向けデータ（リアルタイム系含む、簡潔）
     - 第 4 章：ライセンス台帳（出典表示文言の案、ODbL 混在時の扱い）
     - 第 5 章：取得手順チェックリスト（ローカル環境で実施する順序。ログイン要否・API キー要否を明記）
  2. `data/catalog.json`：第 1〜2 章の各データを `{"source_id":..., "name":..., "publisher":..., "coverage":..., "version":..., "format":..., "crs":..., "url":..., "license":..., "commercial_use":"yes|unknown|restricted", "feeds_fields":[...], "confidence":"confirmed|estimated|unknown", "verification_ids":[...], "fetched_at":null}` の配列で
  3. `docs/05_検証事例集.md`：調査 D の被災事例を、要件定義 12-1 の区分（設備起因の長期停止 / 短期復旧 / 非被災 / 対策済み）で整理。各事例に：日付、施設、所在地（区市）、用途分類（`docs/03_スコアリング仕様.md` 第 2 章の usage_class）、浸水した設備と階、停止期間、本システムで期待する優先度（A/B）、必要データ（PLATEAU のどの区データか）、出典 URL。PoC エリア外の事例はその旨を明記。
- 受入基準：
  - [ ] 04 の第 1 章に要件定義 6-1 の「必須」5 件がすべて載っている
  - [ ] すべての行に確からしさと出典 URL がある
  - [ ] `data/catalog.json` が `python3 -c "import json;json.load(open('data/catalog.json'))"` で読める
  - [ ] 05 に国内事例が 5 件以上、うち PoC エリア（目黒区・世田谷区）の事例が明示されている
  - [ ] 推測には「推定」「未確認」と明記
- 禁止事項：要件定義・スコアリング仕様の変更。URL の捏造（不明なら「未確認」）。
- 完了報告に含めること：作成ファイル一覧、受入基準の自己チェック結果、統合中に見つかったレポート間の矛盾。
