# flood-bcp-risk — Claude Code 向けプロジェクト指針

## このリポジトリは何か

浸水時事業停止リスク診断（PoC）。オープンデータから「浸水したら建物機能・事業が長期停止しやすい建物」を建物単位で一次スクリーニングする。詳細は `README.md` と `docs/02_要件定義書.md`。

## 最初に読む文書

1. `docs/02_要件定義書.md`（決定事項・スコープ・未検証事項 V-xx）
2. `docs/03_スコアリング仕様.md`（実装の唯一の根拠。閾値は `config/*.json`）
3. `docs/06_設計判断ログ.md`（曖昧点への判断の記録）
4. `docs/00_オーケストレーション計画.md`（上位モデルが計画・監督、下位モデルが実装する運用）

## 作業ルール

- 設計判断・仕様変更は `docs/02`・`docs/03` を更新し、理由を `docs/06_設計判断ログ.md` に追記する。コードだけ変えて仕様を放置しない。
- スコアリング閾値・マトリクスはコードに埋め込まず `config/scoring_v0.1.0.json` / `config/measures.json` を変更する。数値を変えたら `score_version` を上げ、`docs/03` の改訂履歴に書く。
- `floodbcp/` は Python 標準ライブラリのみ（NFR-07）。外部依存は `pipelines/`（GIS 処理）に限定し、各スクリプト冒頭の `Requires:` に明記する。
- 推測と確認済み事実を混ぜない。URL・数値の出典を書く。未検証は「未検証」と明記する。
- サンプルデータは架空のみ。実在の施設名・住所を入れない。
- 出力・画面には免責（一次スクリーニングであり安全性を保証しない）と出典表示を必ず付ける（NFR-08）。
- コミットメッセージにモデル名を入れない。

## テスト

```bash
python3 -m unittest discover -s tests            # スコアリングエンジン
python3 -m unittest discover -s pipelines/tests  # ETL の純 Python 部分
node --test 'web/tests/*.test.js'                # ビューア
```

## 現在のマイルストーン

M1：データ検証。`docs/07_M1検証手順書.md` に沿って PLATEAU・東京都浸水予想区域図・DEM を取得し、V-01〜V-12 を確認して `docs/04_データカタログ.md` と `docs/03` を更新する。実データの取得と GIS 処理はネットワーク制限のないローカル環境で行う。
